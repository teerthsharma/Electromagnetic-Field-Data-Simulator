[package]
name = "em-field-sim"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Topological electromagnetic field data simulator using Faraday tensor computations"

[features]
default = ["std"]
std = ["alloc"]
alloc = []
no_std = ["alloc"]

[dependencies]
libm = { workspace = true }

[dev-dependencies]

[lib]
name = "em_field_sim"
path = "src/lib.rs"
